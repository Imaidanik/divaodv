% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diva_plot_odv.R
\name{diva_plot_odv}
\alias{diva_plot_odv}
\title{ODV-style depth-time section plot}
\usage{
diva_plot_odv(
  df,
  var,
  time_corr = 15,
  depth_corr = 10,
  epsilon2 = 0.01,
  transform = "none",
  max_depth = NULL,
  depth_resolution = 1,
  time_resolution = 365,
  log_constant = 1e-10,
  palette = "odv",
  mask_beyond_corr = FALSE,
  contour_binwidth = 1,
  label_binwidth = NULL,
  label_gap = 0,
  sample_points = TRUE,
  zlim = NULL,
  title = NULL,
  y_label = "Depth (m)",
  x_label = NULL,
  fill_label = NULL,
  return_data = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{df}{Tibble with \code{Date} (Date), \code{depth} (numeric m), and
the variable column.}

\item{var}{Character. Variable column name.}

\item{time_corr}{Numeric. Temporal correlation length in \strong{days}. Default 15.}

\item{depth_corr}{Numeric. Depth correlation length in metres. Default 10.}

\item{epsilon2}{Numeric. DIVAnd signal-to-noise ratio. Default 0.01.}

\item{transform}{Character. \code{"none"} (default) or \code{"log"}.}

\item{max_depth}{Numeric or NULL. Maximum grid depth in metres. NULL
auto-derives from non-NA observations.}

\item{depth_resolution}{Numeric. Grid spacing in metres (default 1).
Increasing to 2 halves the depth dimension.}

\item{time_resolution}{Numeric. Grid points per year (default 365).
Use 180 for ~bi-daily, 52 for weekly, 12 for monthly.}

\item{log_constant}{Numeric. Constant added before log transform. Default 1e-10.}

\item{palette}{Character or vector. \code{"odv"} (default), \code{"viridis"},
or a custom hex colour vector. Palette is always reversed for fill.}

\item{mask_beyond_corr}{Logical. Mask cells outside the
±\code{time_corr}-day observation envelope. Default FALSE.}

\item{contour_binwidth}{Numeric. Binwidth for \code{geom_contour} and
\code{geom_text_contour}. Default 1.}

\item{label_binwidth}{Numeric. Binwidth for contour labels (should be a
multiple of \code{contour_binwidth}). Default same as
\code{contour_binwidth}.}

\item{label_gap}{Numeric. Minimum gap between contour labels passed to
\code{metR::geom_text_contour(skip)}. Default 0 (no extra skipping).}

\item{sample_points}{Logical. Overlay observation locations as small black
dots. Default TRUE.}

\item{zlim}{Numeric(2) or NULL. Colour scale limits.}

\item{title}{Character or NULL. Plot title; defaults to \code{var}.}

\item{y_label}{Character. Y-axis label. Default \code{"Depth (m)"}.}

\item{x_label}{Character. X-axis label. Default \code{NULL} (no label).}

\item{fill_label}{Character. Legend title. Default \code{var}.}

\item{return_data}{Logical. Return grid tibble instead of plot. Default FALSE.}

\item{verbose}{Logical. Print progress. Default TRUE.}
}
\value{
A \code{ggplot} object, or a tibble when \code{return_data = TRUE}.
}
\description{
Runs DIVAnd interpolation on a single variable using year-scaled time
(~365 grid points per year) and renders a depth x time raster with
contour lines and contour labels, matching the Ocean Data View aesthetic.
}
