% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/odv_contours.R
\name{odv_contours}
\alias{odv_contours}
\title{Set the contour lines on an ODV section plot}
\usage{
odv_contours(
  breaks = NULL,
  binwidth = NULL,
  labels = TRUE,
  label_gap = 0,
  colour = "black",
  alpha = 0.8,
  linewidth = 0.5,
  ...
)
}
\arguments{
\item{breaks}{Numeric vector or NULL. Explicit contour levels, e.g.
\code{c(20, 22, 24)}. Takes precedence over \code{binwidth}.}

\item{binwidth}{Numeric or NULL. Even spacing between contours, used only
when \code{breaks} is NULL. Defaults to 1.}

\item{labels}{Logical. Label every contour drawn. Default TRUE.}

\item{label_gap}{Numeric. Minimum gap between labels, passed to
\code{metR::geom_text_contour(skip = )}. Default 0.}

\item{colour}{Character. Contour line colour. Default \code{"black"}.}

\item{alpha}{Numeric. Contour line alpha. Default 0.8.}

\item{linewidth}{Numeric. Contour line width. Default 0.5.}

\item{...}{Further arguments passed to \code{\link[ggplot2]{geom_contour}}.}
}
\value{
An object added to a ggplot with \code{+}.
}
\description{
Replaces any contour layers already on the plot with a new set. Because
\code{diva_plot_odv()} returns an ordinary ggplot object, adding this does
\strong{not} re-run DIVAnd -- the interpolated grid is already in the plot,
so contours can be iterated as cheaply as colour mapping.
}
\details{
Existing contour layers are always removed first, so calling this on a plot
built with \code{add_contours = TRUE} leaves one set of contours, not two.
}
\examples{
\dontrun{
p <- diva_plot_odv(df, "Temp", time_corr = 20, depth_corr = 15)

p + odv_contours(breaks = c(20, 22, 24))     # explicit levels
p + odv_contours(binwidth = 2)               # even spacing
p + odv_contours(breaks = 22, labels = FALSE) # one unlabelled isotherm
}

}
\seealso{
\code{\link{odv_contours_remove}}, \code{\link{diva_plot_odv}}
}
