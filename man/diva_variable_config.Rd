% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/variable_config.R
\name{diva_variable_config}
\alias{diva_variable_config}
\title{Construct a DIVA variable configuration object}
\usage{
diva_variable_config(
  var,
  transform = "none",
  time_corr,
  depth_corr,
  epsilon2 = 0.01,
  category = NULL
)
}
\arguments{
\item{var}{Character. Column name of the variable in the data tibble.}

\item{transform}{Character. Transformation to apply before DIVA interpolation.
Must be \code{"none"} or \code{"log"}. Default \code{"none"}.}

\item{time_corr}{Numeric. Temporal correlation length in days. Must be > 0.}

\item{depth_corr}{Numeric. Depth correlation length in metres. Must be > 0.}

\item{epsilon2}{Numeric. Signal-to-noise ratio parameter for DIVAnd.
Must be > 0. Default \code{0.01}.}

\item{category}{Character or NULL. Semantic category label — one of
\code{"physical"}, \code{"chemical"}, \code{"biological"}, or \code{NULL}
(default). Used for faceting and labelling in downstream workflows.}
}
\value{
A named list of class \code{"diva_variable_config"} with elements
\code{var}, \code{transform}, \code{time_corr}, \code{depth_corr},
\code{epsilon2}, and \code{category}.
}
\description{
Creates a validated configuration list for a single oceanographic variable,
specifying the DIVA interpolation parameters and optional transformation.
Useful for multi-variable workflows where you loop over a list of configs.
}
\examples{
# Physical variable — no transform, longer correlation lengths
cfg_temp <- diva_variable_config(
  var        = "Temp",
  transform  = "none",
  time_corr  = 20,
  depth_corr = 15,
  epsilon2   = 0.01,
  category   = "physical"
)

# Biological variable — log transform, shorter correlations
cfg_chl <- diva_variable_config(
  var        = "Chl_a",
  transform  = "log",
  time_corr  = 7,
  depth_corr = 10,
  epsilon2   = 0.02,
  category   = "biological"
)

# Use in a multi-variable loop
configs <- list(cfg_temp, cfg_chl)
for (cfg in configs) {
  cat(cfg$var, "— L_t:", cfg$time_corr, "d, L_z:", cfg$depth_corr, "m\n")
}

}
